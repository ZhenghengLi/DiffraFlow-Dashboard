export enum MetricsType {
    sender = 'sender',
    combiner = 'combiner',
    dispatcher = 'dispatcher',
    ingester = 'ingester',
    monitor = 'monitor',
    controller = 'controller',
    none = 'none',
}
